"use client";

export default function ChocolateAnimation() {
  return (
    <div className="flex items-center gap-3" aria-hidden="true">
      <span className="text-gold/60 text-xs tracking-[0.3em]">✦</span>
      <span className="font-display text-sm italic text-muted/40 tracking-widest">SC</span>
      <span className="text-gold/60 text-xs tracking-[0.3em]">✦</span>
    </div>
  );
}
